name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Pull Request Review
      uses: Man-Jain/mesg-pr-action@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
